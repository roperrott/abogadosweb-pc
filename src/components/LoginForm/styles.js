import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
  loginWrapper: {
    display: 'flex',
    flexDirection: 'column',
    width: '30%',
    padding: '40px 20px',
    margin: '30px auto',
  },
});
